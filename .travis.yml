language: php
php: '7.0'

before_script:
  - composer install

script:
  - if [[ "$WITH_COVERAGE" == "true" ]]; then xdebug-enable; fi
 # - docker run --rm -v $PWD:/app phpqa/phpcs .
  - docker run --rm -v $PWD:/app phpqa/security-checker security:check

deploy:
  provider: script
  skip_cleanup: true
  script: rsync -r --delete-after --quiet $TRAVIS_BUILD_DIR/<dir> u231561718@185.224.137.4:65002:public_html
  on:
    repo: biggiesmall/IDD5IW2
    branch: feature/travis

notifications:
  email:
    recipients:
      - amatroud0@gmail.com
    on_success: always  # default: change
    on_failure: never # default: always